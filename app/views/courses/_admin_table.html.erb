<h1 class="pull-left">Manage Courses</h1>
<table class="table table-hover">
  <thead>
  <tr>
    <th>Code</th>
    <th colspan="2">Name</th>
    <th style="text-align:left;" colspan="2">Lecturer</th>
    <th style="text-align:right;" colspan="2">Students Attending</th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @courses.each do |course| %>
      <tr>
        <td><%= course.code %></td>
        <td colspan="2"><%= link_to course.name, course %></td>
        <td style="text-align:left;" colspan="2"><%= "#{course.lecturer.surname} #{course.lecturer.name}" %></td>
        <td style="text-align:right;" colspan="2"><%= course.students.count %></td>
        <td><%= link_to 'Edit', edit_course_path(course), :class => "btn btn-warning btn-block", :id => "activator-#{course.id}" %></td>
        <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?'}, :class => "btn btn-danger btn-block" %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<%= will_paginate @courses, :class => "apple_pagination" %>
<%= link_to 'New Course', new_course_path, :class => "btn btn-success btn-block" %>
