<%= form_for [course, course.announcements.build], :html => {:id => "new_ann_form"}, remote: true do |f| %>
    <!-- <% if @course.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

        <ul>
        <% @course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %> -->
  <div class="panel">
    <div class="input-group input-group-lg">
      <span class="input-group-addon" id="sizing-addon1">Title</span>
      <%= f.text_field :title, class: "form-control", id: "new-ann-title" %>
    </div>
  </div>
  <div class="panel">
    <div class="input-group input-group-lg">
      <span class="input-group-addon" id="sizing-addon1">Message</span>
      <%= f.text_area :message, class: "form-control", id: "new-ann-message" %>
    </div>
  </div>

  <div class="panel">
    <div class="actions">
      <%= f.submit "Submit", class: "btn bnt-lg btn-success btn-block"%>
    </div>
  </div>
<% end %>
